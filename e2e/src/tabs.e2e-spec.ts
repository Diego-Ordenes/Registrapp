/*import { browser, element, by } from "protractor";


describe("Mi Prueba", () => {
    //Codigo de Configuracion
    beforeEach(() => {
    browser.get("/");
    });
    //Prueba 1
    it("La Tab 1 se muestra por defecto", () => {
        expect(element(by.css(".tab-selected ionlabel")).getText()).toContain("Tab 1");
    });
    it("El usuario puede navegar a la pestaña Tab 2",async ()=>{
        await element(by.css("[tab=tab2]")).click();
        browser.driver.sleep(500);
        expect(element(by.css(".tab-selected ionlabel")).getText()).toContain("Tab 2");
    });
});
*/